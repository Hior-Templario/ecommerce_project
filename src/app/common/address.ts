export class Address { // Define una clase llamada "Address" que será exportada para ser utilizada en otros archivos.

    street: number; // Propiedad "street" que almacena el número de la calle. Es de tipo número.
    city: string; // Propiedad "city" que almacena el nombre de la ciudad. Es de tipo cadena de texto.
    state: string; // Propiedad "state" que almacena el estado o región. Es de tipo cadena de texto.
    country: string; // Propiedad "country" que almacena el nombre del país. Es de tipo cadena de texto.
    zipCode: string; // Propiedad "zipCode" que almacena el código postal. Es de tipo cadena de texto.

}
