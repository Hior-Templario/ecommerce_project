export class State {  
    // Define una clase llamada "State" que será exportada para ser utilizada en otros archivos.
    // Representa un estado o provincia, con un identificador único y un nombre.

    id: number;
    // Propiedad "id" que almacena un identificador único para el estado. Es de tipo número.

    name: string;
    // Propiedad "name" que almacena el nombre del estado. Es de tipo cadena (string).
}
